return {
    dir = "~/code/plugins/minimal-ai.nvim/",
    dependencies = { "nvim-lua/plenary.nvim" },
    keys = { "<leader>ai" }, --- Make sure keys include the key you provide to setup()
    config = function()
        require("minimal-ai").setup()
    end
}
